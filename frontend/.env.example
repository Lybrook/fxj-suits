# Leave blank when using the Next.js rewrite proxy to Django.
NEXT_PUBLIC_DJANGO_API_URL=/api/backend
DJANGO_API_ORIGIN=http://127.0.0.1:8000

# Optional browser push key. Provider secrets belong in backend/.env only.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
